# 💊 Диабет-Маркет

Торговая площадка для покупки и продажи товаров для диабетиков. Аналог Авито, но специализированная для медицинских товаров.

## 🚀 Быстрый старт

### Требования
- Docker и Docker Compose
- Node.js 18+ (для локальной разработки)
- MongoDB (если не используете Docker)

### С использованием Docker (рекомендуется)

```bash
# Запустить весь стек
docker-compose up -d

# Backend будет доступен на http://localhost:5000
# Frontend будет доступен на http://localhost:3000
# MongoDB будет доступен на http://localhost:27017
```

### Локальная разработка

#### Backend

```bash
cd backend

# Установить зависимости
npm install

# Запустить в режиме разработки
npm run dev

# Сервер запустится на http://localhost:5000
```

#### Frontend

```bash
cd frontend

# Установить зависимости
npm install

# Запустить в режиме разработки
npm run dev

# Приложение запустится на http://localhost:3000
```

## 📋 Структура проекта

```
diabetic-marketplace/
├── backend/              # Express + TypeScript сервер
│   ├── src/
│   │   ├── controllers/  # Логика обработки запросов
│   │   ├── models/       # Mongoose схемы
│   │   ├── routes/       # API маршруты
│   │   ├── services/     # Бизнес-логика
│   │   ├── middleware/   # Express middleware
│   │   └── config/       # Конфигурация
│   └── Dockerfile
├── frontend/             # Next.js приложение
│   ├── src/
│   │   ├── pages/        # Страницы приложения
│   │   ├── components/   # React компоненты
│   │   ├── hooks/        # Custom React hooks
│   │   ├── services/     # API клиент
│   │   └── types/        # TypeScript типы
│   └── Dockerfile
└── docker-compose.yml
```

## 🔑 Основные функции

### Для покупателей
- 🔍 **Поиск и фильтрация** товаров по названию, городу, категории и цене
- ❤️ **Сохранение избранного** объявлений
- ⭐ **Просмотр рейтинга** продавцов
- 💬 **Общение** с продавцами
- 📋 **Детальное описание** товара с фото

### Для продавцов
- ➕ **Создание объявлений** с фото и описанием
- ✏️ **Редактирование** своих объявлений
- 🗑️ **Удаление** объявлений
- 📊 **Просмотр статистики** объявлений
- 👤 **Профиль продавца** с рейтингом

## 🛠️ API Endpoints

### Аутентификация
- `POST /api/auth/register` - Регистрация
- `POST /api/auth/login` - Вход
- `GET /api/auth/profile` - Получить профиль

### Объявления
- `GET /api/listings` - Получить все объявления (с фильтрацией)
- `GET /api/listings/:id` - Получить объявление по ID
- `POST /api/listings` - Создать объявление
- `PUT /api/listings/:id` - Обновить объявление
- `DELETE /api/listings/:id` - Удалить объявление
- `POST /api/listings/:id/save` - Сохранить объявление
- `POST /api/listings/:id/unsave` - Удалить из сохранённых

### Пользователи
- `GET /api/users/:userId/listings` - Получить объявления пользователя
- `GET /api/users/saved` - Получить сохранённые объявления

## 🗄️ База данных

### Коллекции

**Users**
```javascript
{
  email: String,
  password: String (hashed),
  name: String,
  phone: String,
  city: String,
  avatar: String,
  rating: Number,
  reviewsCount: Number,
  isSeller: Boolean,
  createdAt: Date,
  updatedAt: Date
}
```

**Listings**
```javascript
{
  title: String,
  description: String,
  category: String,
  price: Number,
  images: [String],
  sellerId: ObjectId,
  city: String,
  status: String,
  views: Number,
  savedBy: [ObjectId],
  rating: Number,
  reviewsCount: Number,
  createdAt: Date,
  updatedAt: Date
}
```

## 🔐 Переменные окружения

### Backend (.env)
```
PORT=5000
MONGODB_URI=mongodb://localhost:27017/diabetic-marketplace
JWT_SECRET=your-secret-key
JWT_EXPIRE=7d
NODE_ENV=development
```

### Frontend (.env.local)
```
NEXT_PUBLIC_API_URL=http://localhost:5000/api
```

## 📱 Категории товаров

- Глюкометры
- Тест-полоски
- Иглы и ланцеты
- Инсулиновые ручки
- Насосы
- Сенсоры
- Прочее

## 🧪 Тестирование

### Backend
```bash
cd backend
npm test
```

### Frontend
```bash
cd frontend
npm test
```

## 📦 Сборка

### Backend
```bash
cd backend
npm run build
```

### Frontend
```bash
cd frontend
npm run build
```

## 🚢 Production deployment

```bash
# Собрать Docker образы
docker-compose build

# Запустить контейнеры в фоне
docker-compose up -d

# Посмотреть логи
docker-compose logs -f

# Остановить контейнеры
docker-compose down
```

## 🤝 Технологии

### Backend
- Express.js - Web фреймворк
- TypeScript - Типизированный JavaScript
- MongoDB - NoSQL база данных
- Mongoose - ODM для MongoDB
- JWT - Аутентификация
- bcryptjs - Хеширование паролей

### Frontend
- Next.js - React фреймворк
- TypeScript - Типизированный JavaScript
- Tailwind CSS - CSS фреймворк
- Axios - HTTP клиент

## 📝 Лицензия

MIT

## 👨‍💻 Автор

Создано как учебный проект торговой площадки для диабетических товаров

   cd backend
   ```
2. Запустите сервер:
   ```
   npm start
   ```

### Клиентская часть
1. Перейдите в директорию клиентской части:
   ```
   cd frontend
   ```
2. Запустите клиентское приложение:
   ```
   npm start
   ```

## Лицензия
Этот проект лицензирован под MIT License.