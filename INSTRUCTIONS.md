# 📋 Инструкция по использованию

## 1. Запуск приложения

### Вариант 1: С Docker (рекомендуется)

```bash
# Перейдите в директорию проекта
cd /Users/timur.v.nikolaev/VS\ Code/diabetic-marketplace

# Запустите приложение
docker-compose up -d

# Проверьте статус контейнеров
docker-compose ps
```

Приложение будет доступно на:
- **Frontend**: http://localhost:3000
- **Backend**: http://localhost:5000
- **MongoDB**: localhost:27017

### Вариант 2: Локальная разработка

#### Запуск Backend

```bash
cd backend
npm install
npm run dev
```

Backend запустится на http://localhost:5000

#### Запуск Frontend (в новом терминале)

```bash
cd frontend
npm install
npm run dev
```

Frontend запустится на http://localhost:3000

## 2. Использование приложения

### Первый запуск

1. Откройте http://localhost:3000
2. Нажмите на "Регистрация"
3. Заполните форму регистрации:
   - Email: ваша почта
   - Пароль: безопасный пароль
   - Имя: ваше имя
   - Телефон: ваш номер
   - Город: ваш город

4. После регистрации вас автоматически вернёт на главную

### Просмотр объявлений

На главной странице вы можете:
- 🔍 **Поиск** объявлений по названию
- 📍 **Фильтр по городу**
- 📂 **Выбор категории**
- 💰 **Фильтр по цене** (от и до)
- ❤️ **Сохранить избранное**

### Просмотр деталей объявления

- Нажмите на любую карточку объявления
- Увидите полную информацию, фото и информацию о продавце
- Кнопка **"❤️ Сохранить"** для добавления в избранное
- Кнопка **"📞 Написать продавцу"** для контакта

## 3. Управление приложением

### С помощью скрипта manage.sh

```bash
# Запустить приложение
./manage.sh start

# Остановить приложение
./manage.sh stop

# Посмотреть логи
./manage.sh logs

# Пересобрать контейнеры
./manage.sh rebuild

# Очистить все ресурсы
./manage.sh clean

# Справка
./manage.sh help
```

### Вручную с Docker

```bash
# Запуск контейнеров
docker-compose up -d

# Остановка контейнеров
docker-compose down

# Просмотр логов
docker-compose logs -f

# Просмотр статуса
docker-compose ps

# Удаление всех данных
docker-compose down -v
```

## 4. Решение проблем

### Приложение не запускается

1. Убедитесь, что Docker установлен:
   ```bash
   docker --version
   docker-compose --version
   ```

2. Проверьте, что порты свободны:
   - 3000 (frontend)
   - 5000 (backend)
   - 27017 (MongoDB)

3. Посмотрите логи:
   ```bash
   docker-compose logs backend
   docker-compose logs frontend
   docker-compose logs mongodb
   ```

### MongoDB не подключается

```bash
# Проверьте статус MongoDB контейнера
docker-compose logs mongodb

# Перезагрузите контейнер
docker-compose restart mongodb
```

### Frontend не видит Backend

1. Проверьте переменную окружения в `frontend/.env.local`:
   ```
   NEXT_PUBLIC_API_URL=http://localhost:5000/api
   ```

2. Перезагрузите браузер и очистите кеш (Ctrl+Shift+Delete)

### Ошибка "Port already in use"

```bash
# Найти процесс, использующий порт (например, 3000)
lsof -i :3000

# Завершить процесс
kill -9 <PID>
```

## 5. Разработка

### Добавление новых зависимостей

#### Backend

```bash
cd backend
npm install <package-name>
# Пересоберите контейнер
docker-compose build backend
```

#### Frontend

```bash
cd frontend
npm install <package-name>
# Пересоберите контейнер
docker-compose build frontend
```

### Изменение кода

Изменения кода автоматически перезагружаются в режиме разработки благодаря hot reload.

### Просмотр базы данных

```bash
# Подключиться к MongoDB в контейнере
docker exec -it diabetic-marketplace-mongo mongosh -u admin -p password

# Выбрать базу данных
use diabetic-marketplace

# Просмотреть коллекции
db.getCollectionNames()

# Просмотреть документы
db.users.find()
db.listings.find()
```

## 6. Production deployment

### Собрать для production

```bash
# Backend
cd backend
npm run build

# Frontend
cd frontend
npm run build
```

### Развернуть с Docker

```bash
# Собрать образы
docker-compose build

# Запустить в background
docker-compose up -d

# Проверить логи
docker-compose logs -f
```

## 7. Остановка приложения

```bash
# Остановить контейнеры без удаления данных
docker-compose down

# Остановить контейнеры и удалить данные
docker-compose down -v

# Удалить контейнеры и образы
docker-compose down --rmi all
```

## 8. Полезные команды

```bash
# Посмотреть все контейнеры
docker ps -a

# Посмотреть размер контейнеров
docker ps --size

# Посмотреть сетевые подключения
docker network ls

# Запустить команду в контейнере
docker exec -it <container-name> <command>

# Скопировать файл из контейнера
docker cp <container-name>:<path> <local-path>
```

## 9. Документация

- [README.md](README.md) - Полная информация о проекте
- [API.md](API.md) - Документация API endpoints
- [QUICKSTART.md](QUICKSTART.md) - Быстрый старт
