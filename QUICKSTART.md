# Быстрый старт 🚀

## Запуск с Docker (рекомендуется)

```bash
# В корне проекта запустите:
docker-compose up -d

# Приложение будет доступно на:
# - Frontend: http://localhost:3000
# - Backend API: http://localhost:5000
# - MongoDB: localhost:27017
```

## Остановка

```bash
docker-compose down
```

## Локальная разработка (без Docker)

### Backend

```bash
cd backend
npm install
npm run dev
# Backend запустится на http://localhost:5000
```

### Frontend (в другом терминале)

```bash
cd frontend
npm install
npm run dev
# Frontend запустится на http://localhost:3000
```

## Тестовые данные

В приложении доступны следующие категории:
- Глюкометры
- Тест-полоски
- Иглы и ланцеты
- Инсулиновые ручки
- Насосы
- Сенсоры
- Прочее

## Основные возможности

✅ Регистрация и вход
✅ Просмотр списка объявлений
✅ Поиск и фильтрация
✅ Просмотр детальной информации об объявлении
✅ Сохранение избранного
✅ Создание объявлений (после авторизации)

## Переменные окружения

Уже предварительно настроены в:
- `backend/.env`
- `frontend/.env.local`

Убедитесь, что MongoDB доступна на `mongodb://admin:password@mongodb:27017`
